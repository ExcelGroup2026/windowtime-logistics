{
  "name": "windowtime-logistics",
  "version": "1.0.0",
  "description": "WindowTime - Logistics Window Time Tracking",
  "main": "index.html",
  "scripts": {
    "start": "npx serve -s public -l 3000",
    "build": "rm -rf dist && mkdir -p dist && cp index.html _headers dist/"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
}
